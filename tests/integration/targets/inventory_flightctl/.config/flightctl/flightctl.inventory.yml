---
plugin: flightctl.core.flightctl
flightctl_config_file: tests/integration/integration_config.yml
# Or, if your plugin supports it:
# config_file: /path/to/your/client.yaml: tests/integration/integration_config.yml
# Use environment variables for auth/config
# e.g. export FLIGHTCTL_SERVICE_URL and FLIGHTCTL_TOKEN
# If credentials are only in client.yaml, plugin may auto-detect it.

# flightctl_validate_certs: false
# verify_ssl: false

# additional_groups:
#   - name: ansible_integration_test_fleet
#     field_selectors:
#       - metadata.owner = "Fleet/ansible-integration-test-fleet"
#   - name: dev_devices
#     field_selectors:
#       - metadata.owner = "Fleet/fleet-dev"
#   - name: test_devices
#     field_selectors:
#       - metadata.owner = "Fleet/fleet-test"
#   - name: unmanaged_devices
#     field_selectors:
#       - metadata.owner = null
#   - name: device_specific_group
#     field_selectors:
#       - metadata.name in ('ansible-integration-test-device-label-1', 'device-dev-02')

# request_timeout: 120
